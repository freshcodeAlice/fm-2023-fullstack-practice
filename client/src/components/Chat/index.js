import React from 'react';
import styles from './Chat.module.css';

const Chat = () => {
    return (
        <div className={styles.chat}>
            (тут буде чат)
        </div>
    );
}

export default Chat;
